# App Store metadata — English (en-US)

## App name (≤30)

```text
kilde: Screen & Voice Recorder
```

## Subtitle (≤30)

```text
System audio + mic, one file
```

## Description

```text
kilde records your screen and audio together — an open-source screen and audio
recorder for macOS. It captures the system audio that the built-in screen
recording can't, in one click.

FOR MEETINGS
Record Zoom, Google Meet, or Teams meetings with the other participants'
voices (system audio) and your own voice (microphone) mixed into a single
file. Pick a window and only that app's audio is recorded — notification
sounds and audio from other apps stay out.

FEATURES
- Screen + system audio with zero setup (native ScreenCaptureKit)
- Record a microphone or devices like BlackHole at the same time (mixed or
  separate tracks)
- Window capture — audio is scoped to that app
- Audio-only recording (M4A), no extra driver needed
- Monitor mode: listen while you record (with BlackHole)
- HDR capture (macOS 15+, HEVC Main10)
- Whether you stop recording or quit the app, your file is always finalized

PRIVACY
Your recordings never leave your Mac. The developer only receives aggregated
usage statistics such as launch counts — never the content of your recordings
or the files themselves. See PRIVACY.md at github.com/kilde-team/kilde.

REQUIREMENTS
macOS 14 or later (Apple Silicon). Free, no ads, open source (MIT).

The GUI shares the same recording engine as the `kilde` command-line tool.
Learn more at https://github.com/kilde-team/kilde
```

## Keywords (≤100)

```text
screen recorder,system audio,meeting,record,Zoom,audio recording,microphone,window capture,mp4
```

## What's New (template)

```text
Summarize this version's changes in 1–3 lines (fixes and new features).
```
