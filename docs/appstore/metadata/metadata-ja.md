<!-- markdownlint-disable MD013 -- ASC の文面をそのまま写すため折り返さない (ASC は改行をそのまま表示する) -->
# App Store メタデータ — 日本語 (ja)

**ASC の実際の登録内容を写したもの** (2026-09-23 に ASC から転記)。名前・サブタイトル・
キーワードは 0.3.0 で審査を通過し配信中の文面、説明は 0.4.0 用。0.3.0 の文面から 2 か所だけ
変えてある: Firebase Analytics 導入 (#136) に合わせて「データの収集や外部への送信は一切行いません」を
差し替え、MAS 版は設定をアプリコンテナへ移す (issue #126) ため「その設定は CLI の既定値としても
共有されます」を削った。

## アプリ名 (≤30)

```text
Kilde
```

## サブタイトル (≤30)

```text
システム音声もマイクも1本に収録
```

## 説明

```text
kilde は、Mac の画面とシステム音声をまとめて記録するメニューバー常駐のレコーダーです。標準の画面収録では取り込めないアプリの音や通話相手の声を、自分のマイクと一緒に 1 本のファイルへ収録できます。仮想オーディオドライバなどの追加インストールは必要ありません。

■ 3 つの収録モード
・画面：ディスプレイ全体を収録します。接続中の複数のディスプレイから選べます。
・ウィンドウ：選んだウィンドウだけを収録します。システム音声もそのアプリの音だけに絞られるため、デスクトップ全体を映さずに共有できます。
・音声のみ：映像を録らず M4A で保存します。打ち合わせの記録や文字起こしの用途に適しています。

■ 音声ソースを自由に組み合わせ
システム音声、既定のマイク、接続中のオーディオインターフェースなどを個別に選んで収録できます。複数のソースを 1 トラックに合成するか、ソースごとに分離して書き出すかを切り替えられるので、編集時に自分の声とアプリの音を別々に扱えます。

■ 録画中もメニューバーだけで完結
経過時間はメニューバーに常時表示されます。ポップオーバーではファイルサイズとソース別の入力レベルメーターを確認できるので、マイクが入っていないまま録り続ける事故を防げます。パネルを閉じても録画は続き、グローバルホットキーで他のアプリを使いながら開始・停止できます。

■ 壊れたファイルを残さない
停止したときはもちろん、アプリを終了したときやアップデートによる再起動時も、書き込み中のファイルを必ずファイナライズしてから終了します。収録し終えたファイルは「最近の録画」から Finder ですぐに開けます。

■ そのほか
・保存先フォルダを自由に指定できます
・ログイン時の自動起動に対応しています
・オープンソース（MIT ライセンス）です。録画の内容や設定を外部へ送信することはありません（アプリの改善のため、Firebase Analytics で匿名の利用統計を、Firebase Crashlytics でクラッシュレポートを収集します）

kilde は macOS 14 以降に対応し、ScreenCaptureKit によるネイティブな収録を行います。

※ 収録には画面収録の許可が、マイクを使う場合はマイクの許可が必要です。会議や通話を記録する際は、参加者の同意を得たうえでご利用ください。
```

## キーワード

```text
画面収録,画面録画,内部音声,システム音声,録音,マイク,会議,ミーティング,議事録,文字起こし,メニューバー,M4A,スクリーンレコーダー
```

(71 文字 / 180 バイト。0.3.0 でこのまま受理・審査通過している — ASC の入力欄の
残数表示は文字数で数える。metadata README の注記参照)

## サポート URL / マーケティング URL

```text
https://kilde.site/ja/
```

## プライバシーポリシー URL

```text
https://kilde.site/ja/privacy/
```

## 新着情報 (0.6.0)

0.5.0 からの変更は内部的なもの (クラッシュレポート収集) だけなので、文面は
正直に短く保つ — 実装に無い機能の訴求を足さない。

```text
安定性と信頼性を改善しました。
```

## 新着情報 (0.8.1)

```text
録画の文字起こしと要約 (オンデバイス処理) に対応しました。さらに、ショートカットからの録音の開始・停止、録画ライブラリの全文検索と該当時刻からの再生、録画 «中» の保存先変更でも文字起こしが正しく書き出される安定性の改善を含みます。
```

## 新着情報 (テンプレート)

```text
この版の変更点を 1〜3 行で書く (バグ修正・追加機能)。
```
